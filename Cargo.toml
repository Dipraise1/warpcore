[package]
name = "warpcore"
version = "0.1.0"
edition = "2021"
description = "A basic Solana parallelism analyzer prototype."

[[bin]]
name = "brain"
path = "src/brain.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
